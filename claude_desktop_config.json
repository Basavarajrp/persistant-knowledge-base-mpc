{
  "_readme": "Paste the mcpServers block into: ~/Library/Application Support/Claude/claude_desktop_config.json — then quit and relaunch Claude Desktop.",

  "mcpServers": {
    "knowledge-graph": {
      "command": "uv",
      "args": [
        "--directory",
        "/Users/basavarajpatil/expolaration/MCP Project/knowledge-graph-mcp-v2",
        "run",
        "knowledge-graph-mcp"
      ],
      "env": {
        "NEO4J_URI":      "bolt://localhost:7687",
        "NEO4J_USER":     "neo4j",
        "NEO4J_PASSWORD": "your_secure_password_here"
      }
    }
  }
}
